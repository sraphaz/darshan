/**
 * Entrada canônica do mapa simbólico — Engine 2.0.
 * Estrutura: jyotish, numerology, themes, traits, evidence.
 * Use buildSymbolicMap(profile) para obter o mapa; depois composeReading(map, topic) para leituras temáticas.
 */

export type { SymbolicMap } from "./types";
export { buildSymbolicMap } from "./builder";
