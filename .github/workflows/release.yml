# Esteira Release: build + artefato + deploy (falha se config ausente)
name: Release

on:
  release:
    types: [published]
  push:
    tags:
      - "v*"

jobs:
  build-and-bundle:
    name: Build & Bundle
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install
        run: npm ci

      - name: Get version
        id: version
        run: echo "version=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT

      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_VERCEL_URL: ""

      - name: Bundle artifact
        run: node scripts/bundle-artifact.js --no-archive

      - name: Create archive
        run: |
          VERSION=${{ steps.version.outputs.version }}
          FOLDER=$(ls -d dist/darshan-* 2>/dev/null | head -1)
          if [ -z "$FOLDER" ]; then echo "Pasta do artefato não encontrada"; exit 1; fi
          BASENAME=$(basename "$FOLDER")
          tar czf "dist/darshan-${VERSION}.tar.gz" -C dist "$BASENAME"
          echo "ARTIFACT_PATH=dist/darshan-${VERSION}.tar.gz" >> $GITHUB_ENV

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-artifact
          path: dist/darshan-*.tar.gz
          retention-days: 30

  # Deploy falha propositalmente se secrets não configurados (Vercel, etc.)
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: build-and-bundle
    if: success()
    steps:
      - name: Check deploy config
        id: check
        run: |
          if [ -z "${{ secrets.VERCEL_TOKEN }}" ] && [ -z "${{ secrets.VERCEL_ORG_ID }}" ]; then
            echo "Deploy não configurado: defina VERCEL_TOKEN e VERCEL_ORG_ID (e VERCEL_PROJECT_ID) nos secrets do repositório para deploy automático."
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Deploy (Vercel)
        if: steps.check.outputs.skip != 'true'
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          working-directory: ./
          vercel-args: "--prod"

      - name: Deploy skipped (config ausente)
        if: steps.check.outputs.skip == 'true'
        run: |
          echo "::error title=Deploy não configurado::Defina VERCEL_TOKEN, VERCEL_ORG_ID e VERCEL_PROJECT_ID nos secrets do repositório (Settings > Secrets) para habilitar deploy automático."
          exit 1
